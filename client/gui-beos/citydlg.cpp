/* citydlg.cpp */
// Multiple city dialogs can be active at once -- one dialog per city

#include <Alert.h>
#include "Defs.hpp"
#include "citydlg.h"


void
popup_city_dialog(struct city *pcity, int make_modal)
{
	NOT_FINISHED( "popup_city_dialog(struct" );
}


void
popdown_city_dialog(struct city *pcity)
{
	NOT_FINISHED( "popdown_city_dialog(struct" );
}


void
popdown_all_city_dialogs(void)
{
	NOT_FINISHED( "popdown_all_city_dialogs(void)" );
}


void
refresh_city_dialog(struct city *pcity)
{
	NOT_FINISHED( "refresh_city_dialog(struct" );
}


void
refresh_unit_city_dialogs(struct unit *punit)
{
	NOT_FINISHED( "refresh_unit_city_dialogs(struct" );
}


