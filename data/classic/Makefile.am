## Process this file with automake to produce Makefile.in

## Override automake so that "make install" puts these in proper place:
pkgdatadir = $(datadir)/@PACKAGE@/data/classic

pkgdata_DATA =          \
	roads.xpm	\
	tiles.xpm	\
	units.xpm

EXTRA_DIST = $(pkgdata_DATA)
